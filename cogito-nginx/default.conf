server {
  listen 80;
  location /static {
    root /var/www;
  }
}

server {
    listen 443 ssl;
    ssl_certificate /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;
    location /static {
        root /var/www;
    }
}
